services:
  voice-notes:
    build: .
    container_name: voice-notes
    restart: unless-stopped
    ports:
      - "8080:8000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
      - DB_PATH=/data/expenses.db
    volumes:
      - notes-data:/data
volumes:
  notes-data: